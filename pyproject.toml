[project]
name = "climail"
version = "0.1.0"
dependencies = [
  "typer",
  "keyring",
  "python-dotenv",
  "platformdirs",
  "google-auth",
  "google-auth-oauthlib",
  "requests"
]

[project.scripts]
climail = "climail.main:app"
