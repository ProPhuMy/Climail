[project]
name = "climail"
version = "0.1.0"
dependencies = [
  "typer",
  "keyring",
  "python-dotenv",
  "platformdirs",
  "google-auth",
  "google-auth-oauthlib",
  "requests",
  "prompt_toolkit"
]

[project.scripts]
climail = "climail.main:app"
